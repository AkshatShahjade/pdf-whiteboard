Functionality Important

- remove artificial smoothening done to the pen tool even more

- tldraw pane shift+scrollwheel doesn't work
- Add in depth explanation of how this works - or link a yt video
-- Also keep a Help menu in the Settings, that lists all the shortcuts, and an in depth explanatio nof all the controls and mechanics and how to use the app. This should be a pop up that is opened when clicked and is very detailed. Even a technical description of how this app works. -- MAnually make this better


- \ - there should also be a short cut that puts the divider between the panes 80% enlargening one pane and shrinking the other. It should enlarge that pane which is in focus, unless that pane is already enlarged (divider is at 80% and pdf is in focsu, or divider is at 20$ and whiteboard is in focus), in which case it should enlarge the other. Do nothing if no region is selected (hence the whiteboard pane isn't even visible). This shortcut isn't working.





















-----------------------------------------------------------------
UI imp
- UI is too dark and hard to read- make more visible, but still dark. Also make all the options and settings that overlay the pdf translucent so you can see the pdf behind it
UI not imp
- Page Numbers, and ability to type in a page number and jump to that (similar to the system of google drive, microsoft edge, basically any pdf viewer). Put this
- Make the Verticle Scroll in the PDF Pane more visible.
- Change Cursor icon of delete, lasso and section tools to be more appropriate. For start section, a downwards facing square bracket, and for end section an upwards facing square bracket as cursors...

- Fix the unreliability of the boundary Ctrl+Click And Drag of the lasso tool shapes. I observe that if the lasso boundary isn't nearly horizontal or vertical, the click and drag won't register so the region won't move. It doesn't work on the slanted edges.
-change keyboard shortcut of section tool to 's'
- Then pressing 's' when the section tool is already active should cancel the section tool and go back to selection tool when in pdf pane (during update or creation modes)
- Either when just Ctrl+Cliking the boundary of rectangle/lasso tool region, or when we Ctrl+Clikc and Dragged the region to a new position and then we leave the mouse. Enter into an edit mode where we can actually change the shape of the region itself (creating a new rectangle, or fully creating a new lasso shape) while the data in the whiteboard will remain attached to the same region even it its shape changes. When this editor mode is enabled for these 2 tools, create 2 temporary buttons: Cancel and Update that pop ups horizontally to the side of the respective tool icon in the toolbox. Only when the update button is clicked will the change finalize. If Esc is pressed during the editor mode or cancel burron is pressed the old region shape is reverted to.

- Keyboard Shortcuts
    - shortcuts must be differentiated based on where the focus is currently, is it in the pdf pane or not. No tldraw shortcuts should activate when tldraw isn't in focus and vice-versa

    - Tldraw: v, d, t, e and the other defaults. All is well here, do not change this functionality. Just that these should only work when this pane is in focus.
    - PDF: letters for each of the tools, (only one for section, not 4 for the four buttons it comprises), also Ctrl+scrollwheel when in pdf focus, should zoom in and out.
    - Esc is working the way I want it
    - there should also be a short cut that puts the divider between the panes 80% enlargening one pane and shrinking the other. It should enlarge that pane which is in focus, unless that pane is already enlarged (divider is at 80% and pdf is in focsu, or divider is at 20$ and whiteboard is in focus), in which case it should enlarge the other. Do nothing if no region is selected (hence the whiteboard pane isn't even visible.)

    -Also, while changing the zoom,  the content at the center of the screen in the pdf (both horizontally and vertically) must remain in the center after the zoom.
    - Also make Shift+Scrollwheel be able to move the document horizontally fully - currently if doesn't move left, but i want it to also move left till the page ends so that the whole page can be seen at any zoom level using the scrollwheel for up and down movement and shift+scrollwheel for left and right - while the default when it first loads should be that it is in the center.

    - after zoom scrollwheel input stops working. It is unreliable. I use it once or twice and then it stops working. I have to go to home and then come back into the document for it to work again. However the buttons always work. Why not just make the scrollwheel do whatever the button does with the 250ms buffer??? Also add Ctrl+0 to reset focus to 100% for the pdf pane...
- Remove the regions counter in the dissappearing Title bar of the App.jsx

- Clear Session Data should only clear the recents.
- Also add a manual backup button along with the export and import buttons that does the same thing autobackup does, but at your command when you press the button.
- Add option to export and import all the data in the Settings menu. It will open file pickers to choose export location, and choose import file... This export / import must include all the data. It should be complete.
- Add Confirm pop up for the delete session data button
-
- - swap the cursor icons for the section tool start and end options.
- pressing esc during the editing / creating process of section tool should cancel the modifcation and revert back. Also add a cancel button after the start and end and confirm buttons . the rightest button
- make the scroll bar in App.jsx pdf pane ligher in color so more visible (just some, not too much)
- The heading bar is notaccessible anmore, it doesn't appear when mouse is brought near, so I am stuck


